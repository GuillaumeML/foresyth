spring:
  datasource:
    url: jdbc:tc:postgresql:latest:///test
    username: test
    password: test
  liquibase:
    enabled: true
    change-log: classpath:/db/changelog/changelog-main.xml
  test:
    database:
      replace: nonce